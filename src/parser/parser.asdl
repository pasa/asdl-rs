root = (type* types)
type = SumType(typeId, constr* constructors)
     | ProdType(typeId, field* fields)
constr = (constrId id, field* fields)
field = Single(typeId, id)
      | Option(typeId, id)
      | Sequence(typeId, id)
typeId = ()
constrId = ()
id = ()
